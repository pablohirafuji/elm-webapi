/*
import Elm.Kernel.Json exposing (unwrap, wrap)
*/

var _BrowserCustom_self = __Json_wrap(self);

var _BrowserCustom_document = __Json_wrap(document);

var _BrowserCustom_window = __Json_wrap(window);

var _BrowserCustom_navigator = __Json_wrap(navigator);
